[[source]]
url = "https://pypi.python.org/simple"
verify_ssl = true
name = "pypi"

[dev-packages]

# Documenting
sphinx = "~=1.8.1"
sphinx_rtd_theme = "~=0.4.2"

# Linting
flake8 = "==3.5.0"  # required for a regression on prospector
prospector = "~=1.1.3"

# Testing
coverage = "~=4.5.1"
nose = "~=1.3.7"
"nose-htmloutput" = "~=0.6.0"
tox = "~=3.5.2"
betamax = "~=0.8.1"

# Releasing
semver = "~=2.8.1"
gitwrapperlib = "~=0.9.1"

# Uploading
twine = "~=1.12.1"

# Logging
coloredlogs = "~=10.0"
emoji = "~=0.5.1"


[packages]

coloredlogs = "~=10.0"

requests = "~=2.21.0"
cachetools = "~=2.0.1"

[requires]
python_version = "3"
